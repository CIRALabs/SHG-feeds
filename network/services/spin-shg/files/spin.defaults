#!/bin/sh

uci batch <<EOF
set mosquitto.mosquitto.port=1883
set mosquitto.mosquitto.protocol=mqtt
set mosquitto.spin_listener=listener
set mosquitto.spin_listener.port=1884
set mosquitto.spin_listener.protocol=websockets
set mosquitto.owrt.use_uci='1'
commit mosquitto
EOF
